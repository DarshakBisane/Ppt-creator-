# ==============================================================================
# PresenAI Frontend Environment Configuration Template
# NOTE: All VITE_* variables are PUBLIC and bundled into browser assets.
# NEVER put private API keys or secrets in this file.
# ==============================================================================

# Backend API Base URL (empty defaults to same-origin / relative path)
VITE_API_BASE_URL=http://localhost:8000
